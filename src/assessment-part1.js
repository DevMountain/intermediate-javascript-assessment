// Part 1:
// Scope,
// Using an existing promise (were checking for use of .then and a callback)
// Context - Implicit and new keyword
// Type checking
// Prototypes

var item1 = 'thing1';

function handleIt(promise) {
  promise .then(function(response) {

  });
}
